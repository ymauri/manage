{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReceptionVoucher</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ entity.details }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ entity.isactive }}</td>
            </tr>
            <tr>
                <th>Forreception</th>
                <td>{{ entity.forreception }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('receptionvoucher') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('receptionvoucher_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
