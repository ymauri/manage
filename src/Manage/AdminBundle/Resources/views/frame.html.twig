{# empty Twig template #}
{% extends 'AdminBundle::base.html.twig' %}
{% block menu%}  
    {% set current_user = app.user %}
    {% if is_granted('ROLE_SUPERADMIN') or is_granted('ROLE_ADMIN') %}
        <li>
            <a href="#"><strong><i>{{ current_user.name }}</i></strong></a>
            <ul class="dl-submenu">
                <li><a href={{path('user_edit', {'id': current_user.id})}}>User Profile</a></li>       
                <li><a href={{path('logout')}}>Logout</a></li>
            </ul>
        </li>
    {% endif %}
    {% if is_granted('ROLE_SUPERADMIN')%}
        <!--<li><a href={{path('user')}}>Workers</a></li>
        <li><a href="#">Stats</a></li>-->
    {% endif %}
    
{% endblock %}
{% block header %}

{% endblock %}

